<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.rayton.gps.dao.baseinfo.areaInDevice.IAreaInDeviceDao">
    <select id="assignedPageVehiclesCount" resultType="java.lang.Integer">
        SELECT COUNT(1)
        FROM equipment_region AS A INNER JOIN equipment AS B ON
        A.DEVICENUMBER = B.DEVICENUMBER
        LEFT JOIN automobile AS C ON
        B.ID = C.DEVICEID
        WHERE AREAID = #{0} AND AREATYPE = #{1}
    </select>
    <select id="assignedPageVehiclesDetail" resultType="com.rayton.gps.dao.baseinfo.areaInDevice.AreaInDeviceInfo">
        SELECT
        A.DEVICENUMBER,
        A.AREAID,
        A.AREATYPE,
        A.TIME,
        C.PLATENUMBER
        FROM
        equipment_region AS A INNER JOIN equipment AS B ON
        A.DEVICENUMBER = B.DEVICENUMBER
        LEFT JOIN automobile AS C ON
        B.ID = C.DEVICEID
        WHERE AREAID = #{0} AND AREATYPE = #{1}
        ORDER BY A.TIME
        LIMIT
        #{2}, #{3}
    </select>
    <insert id="addVehicle">
        REPLACE equipment_region
        SET
        DEVICENUMBER = #{0}, AREAID = #{1}, AREATYPE = #{2}, TIME = NOW()
    </insert>
    <delete id="removeVehicle">
        DELETE FROM equipment_region
        WHERE DEVICENUMBER = #{0} AND
        AREAID = #{1} AND AREATYPE = #{2}
    </delete>
    <insert id="log">
        INSERT INTO equipment_region_log (ID, DEVICENUMBER, AREAID,
        AREATYPE, `ACTION`,
        UNID, USER, SENDTIME) VALUES (#{0}, #{1}, #{2},
        #{3}, #{4}, #{5}, #{6}, NOW())
    </insert>
    <select id="findDevice" resultType="java.lang.String">
        SELECT DEVICENUMBER
        FROM
        equipment_region
        WHERE AREAID = #{0} AND AREATYPE = #{1}
    </select>
    <delete id="deleteAreaInDevice">
        DELETE FROM equipment_region
        WHERE AREAID = #{0} AND
        AREATYPE = #{1}
    </delete>
    <delete id="deleteAreaInMaplayer">
        DELETE FROM map_layer_cover
        WHERE AREAID = #{0} AND
        AREATYPE = #{1}
    </delete>
</mapper>