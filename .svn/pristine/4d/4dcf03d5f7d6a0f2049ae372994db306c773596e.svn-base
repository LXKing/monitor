<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.edata.monitor.dao.cache.area.IAreaCatcherDao">
    <select id="getCircles" resultType="com.edata.monitor.dao.cache.area.AreaInfo">
        SELECT
            A.DEVICENUMBER,
            A.AREAID,
            A.AREATYPE
        FROM areaindevice AS A INNER JOIN circle AS B ON
                                                          A.AREAID = B.ID
        WHERE B.DEVICECATCH = 0
    </select>
    <select id="getRectangles" resultType="com.edata.monitor.dao.cache.area.AreaInfo">
        SELECT
            A.DEVICENUMBER,
            A.AREAID,
            A.AREATYPE
        FROM areaindevice AS A INNER JOIN rectangle AS B
                ON A.AREAID = B.ID
        WHERE B.DEVICECATCH = 0
    </select>
    <select id="getPolygons" resultType="com.edata.monitor.dao.cache.area.AreaInfo">
        SELECT
            A.DEVICENUMBER,
            A.AREAID,
            A.AREATYPE
        FROM areaindevice AS A INNER JOIN polygon AS B ON
                                                           A.AREAID = B.ID
        WHERE B.DEVICECATCH = 0
    </select>
    <select id="getRoutes" resultType="com.edata.monitor.dao.cache.area.AreaInfo">
        SELECT
            A.DEVICENUMBER,
            A.AREAID,
            A.AREATYPE
        FROM areaindevice AS A INNER JOIN route AS B ON
                                                         A.AREAID = B.ID
        WHERE B.DEVICECATCH = 0
    </select>
    <select id="getSectionIds" resultType="java.lang.Long">
        SELECT SECTIONID
        FROM
            routesection
        WHERE
            ROUTEID = #{0}
    </select>
</mapper>